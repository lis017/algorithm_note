check도 크기할당 해야함(map도 하잖아)

bfs에서 cheak, visited의 뜻(기준)
-> 방문직전(q에 넣으면 되는상황)or 방문하자마자 check가 true임.
그 곳에서 4방향 다 체크하거나, 4방향 좌표를 q를 새로 다 넣어야 cheak가 아님

//9328
.에 해당하는 내용은(bfs함수에서) else안쓰고 해야돼!  

string을 바로 char배열에 받기
-> char[] keyList = br.readLine().toCharArray();                     

//2178
map등에 붙은숫자를 chatAt() 으로 넣을경우
-> map[i][j] = line.charAt(j) - '0';
//문자 0 빼기


최단거리 기억안나면
-> dist[][]를 이동거리배열로 써서
map처럼 크기할당하고
q에 추가할때마다 nx ny에 x y +1로 써도됨